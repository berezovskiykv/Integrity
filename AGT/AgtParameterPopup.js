#INCLUDE "ObservablePopup.js"
/**
 * Класс для попапа агрегата
 * @class AgtParameterPopup
 * @extends ObservablePopup
 */
class AgtParameterPopup extends ObservablePopup {
    constructor (publisher, config) {
        super(config);
        let context = this;
    }








}
